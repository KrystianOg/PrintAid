# Database
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=printaid_user
DB_PASS=supersecret
DB_NAME=printaid_test
DATABASE_URL="postgres://$DB_USER:$DB_PASS@$DB_HOST:$DB_PORT/$DB_NAME?sslmode=disable"

# App
PORT=3000
NODE_ENV=test
JWT_SECRET=<your-secret>
FRONTEND_URL=http://localhost:19006    # Expo default

# Email (for nodemailer mocks)
EMAIL_FROM=no-reply@printaid.app

